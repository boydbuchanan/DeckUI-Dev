import { Text } from "@deckai/deck-ui";
import * as CMS from "@deckai/client/types/cms";

export const EditServices = ({}: {}) => {
  return (
    <div className="flex flex-col pt-8 gap-1">
      <Text variant="heading-sm">Coming Soon!</Text>
      <Text variant="body-default">
        Share your service rates and preferred partnership types with
        transparent pricing.
      </Text>
    </div>
  );
};
